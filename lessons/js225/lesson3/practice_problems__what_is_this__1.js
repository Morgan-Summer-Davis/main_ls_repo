// That is dependent on how the function is invoked.

// The global object

// All of these point to the global object

// obj

// This snippet will throw an error, since this refers to undefined in strict mode,
// and undefined cannot have properties

// 2

// foo, then an error as the global object has no baz() fethod