function subtract(initial: number, values: number[]): string {
  // code omitted for brevity
  return ''; // Included to shut TS up
}

'Alice, 30, from USA'
'Bob, unknown age, from Canada'
'Charlie, 25, from UK'